package car.rental.models.enums;

public enum UserRole {
    ADMIN,
    USER
}
// Inventory needs to be managed
// Pricing needs to be managed
// Inventory includes vehicles, locations
// Billing and payments
